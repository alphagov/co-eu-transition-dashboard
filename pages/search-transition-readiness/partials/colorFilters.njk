{% from "govuk/components/checkboxes/macro.njk" import govukCheckboxes %}


{{ govukCheckboxes({
    name: "colorFilter",
    items: [
      {
        id: "colorFilter-" + option.color,
        value: option.color,
        text: option.definition,
        checked: false
      }
    ]
  }) 
}}