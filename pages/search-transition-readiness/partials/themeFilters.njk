{% from "govuk/components/checkboxes/macro.njk" import govukCheckboxes %}


{{ govukCheckboxes({
    name: "themeFilter",
    items: [
      {
        id: option.name,
        value: option.name,
        text: option.name,
        checked: false
      }
    ]
  }) 
}}