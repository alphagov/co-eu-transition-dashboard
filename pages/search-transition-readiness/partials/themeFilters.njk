{% from "govuk/components/checkboxes/macro.njk" import govukCheckboxes %}


{{ govukCheckboxes({
    name: "themeFilter",
    items: [
      {
        value: option.name,
        text: option.name,
        checked: false
      }
    ]
  }) 
}}