      <div class="filter-nav">
        <nav>
        <ul class="govuk-header__navigation" aria-label="Top Level Filters">
          <li class="govuk-header__navigation-item govuk-header__navigation-item">
        {% if option.value in data.filters.projects[filter.id] | default([]) %}
          {% set filterNames = (filterNames.push (filter.name), filterNames) %}
            <span class="filter-block">{{ option.value }}</span>
        {% elif option.list.value in data.filters.projects[filter.id] | default([]) %}
          {% set filterNames = (filterNames.push (filter.name), filterNames) %}
            <span class="filter-block">{{ option.list.value + " - " + option.list.title }}</span>
        {% endif %}
            </li>
          </ul>
        </nav>
      </div>